language: php

php:
  - 5.3
  - 5.4

before_install:
  - composer self-update
  - composer install --dev -n --prefer-source

script:
	- ./vendor/bin/phpunit

notifications:
  email:
    recipients:
      - aluralma@gmail.com
    on_success: change
    on_failure: always